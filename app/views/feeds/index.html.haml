%h1 Listing feeds

%table
  %tr
    %th Url
    %th Name
    %th Feed url
    %th Author
    %th Twitter username
    %th
    %th
    %th

  - @feeds.each do |feed|
    %tr
      %td= feed.url
      %td= feed.name
      %td= feed.feed_url
      %td= feed.author
      %td= feed.twitter_username
      %td= link_to 'Show', feed
      %td= link_to 'Edit', edit_feed_path(feed)
      %td= link_to 'Destroy', feed, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Feed', new_feed_path
